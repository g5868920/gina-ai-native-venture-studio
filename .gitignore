# Dependencies
node_modules/

# Build output
dist/
coverage/

# Derivative outputs — never canonical; promoted only via explicit writeback decision (CLAUDE.md)
generated/

# Environment and secrets — never commit credentials (CLAUDE.md: Data Boundaries)
.env
.env.*

# OS / editor noise
.DS_Store
*.log

# Local Claude Code tool settings (not project source of truth) — .claude/skills/ IS tracked
.claude/settings.local.json
